{
  "name": "blockchain-secure-chart-app",
  "version": "1.0.0",
  "description": "Root workspace for the Blockchain-Enabled Secure Chart Application FYP",
  "private": true,
  "scripts": {
    "install:all":    "npm install --prefix blockchain && npm install --prefix frontend",
    "compile":        "npm run compile --prefix blockchain",
    "test:contracts": "npm test --prefix blockchain",
    "chain":          "npm run node --prefix blockchain",
    "deploy":         "npm run deploy --prefix blockchain",
    "frontend":       "npm run dev --prefix frontend",
    "build":          "npm run build --prefix frontend"
  }
}
